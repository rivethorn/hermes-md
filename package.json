{
  "name": "hermes",
  "version": "0.2.2",
  "description": "A CLI tool for adding and removing Markdown files to and from a Supabase bucket",
  "bin": {
    "hermes": "src/hermes.ts"
  },
  "module": "src/hermes.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/index.ts --compile --minify --outfile hermes"
  },
  "files": [
    "src",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "bun": ">=1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/js-yaml": "^4.0.9"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.89.0",
    "commander": "^14.0.2",
    "dotenv": "^17.2.3",
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.1",
    "slugify": "^1.6.6",
    "toml": "^3.0.0"
  }
}
